subscription {
  Product(filter: {
    mutation_in: [CREATED, UPDATED, DELETED]
  }) {
    mutation
    previousValues {
      objectID: id
    }
    node {
      id
      name
      isHidden
      brand {
        id
        name
        boost
        isHidden
      }
    }
  }
}
